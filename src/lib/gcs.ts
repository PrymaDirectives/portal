// Google Cloud Storage client — implemented in Phase 6
// import { Storage } from "@google-cloud/storage";
// export const storage = new Storage({ projectId: process.env.GCP_PROJECT_ID });
// export const bucket = storage.bucket(process.env.GCS_BUCKET_NAME!);
// Stored at: invoices/{invoiceID}/invoice-v{n}.pdf

export const storage = null; // placeholder until Phase 6
