// PDF generation service — implemented in Phase 6
// Uses @react-pdf/renderer or puppeteer to generate invoice PDFs
// then uploads to GCS via src/lib/gcs.ts
// Stored at: invoices/{invoiceID}/invoice-v{n}.pdf

// export async function generateAndStorePDF(invoiceId: string): Promise<string> { ... }

export const generateAndStorePDF = null; // placeholder until Phase 6
