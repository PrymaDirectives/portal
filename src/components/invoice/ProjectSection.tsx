// ProjectSection — project title and description (Phase 4)
import type { Invoice } from "@/types/invoice";
export function ProjectSection({ invoice }: { invoice: Invoice }) {
  void invoice;
  return <div className="project-section" />;
}
